name: demo_items
description: A new Flutter package project.
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.6.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  editors: any
  combos: ^1.1.4

dependency_overrides:

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
