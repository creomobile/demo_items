name: demo_items
description: A new Flutter package project.
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  editors: any
  combos: any

dependency_overrides:
  combos:
    git:
      url: https://github.com/creomobile/combos
      ref: 1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
